name: Update Formula

on:
  release:
    types:
      - published
      - edited

jobs:
  check-assets:
    runs-on: ubuntu-latest
    outputs:
      has_amd64_sequoia: ${{ steps.check.outputs.has_amd64_sequoia }}
      has_arm64_sequoia: ${{ steps.check.outputs.has_arm64_sequoia }}
    steps:
      - name: Check available assets
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ github.event.release.tag_name }}
        run: |
          gh release view "${VERSION}" --repo "${{ github.repository }}" --json assets --jq '.assets[].name' > assets.txt
          cat assets.txt

          grep -q "pinc-${VERSION}.amd64_sequoia.bottle.tar.gz" assets.txt \
            && echo "has_amd64_sequoia=true" >> $GITHUB_OUTPUT \
            || echo "has_amd64_sequoia=false" >> $GITHUB_OUTPUT

          grep -q "pinc-${VERSION}.arm64_sequoia.bottle.tar.gz" assets.txt \
            && echo "has_arm64_sequoia=true" >> $GITHUB_OUTPUT \
            || echo "has_arm64_sequoia=false" >> $GITHUB_OUTPUT

  update-formula:
    needs: check-assets
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ github.event.release.tag_name }}
      BASE_URL: 'https://github.com/${{ github.repository }}/releases/download/${{ github.event.release.tag_name }}'
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: main

      - name: Download assets
        run: |
          gh release download "${VERSION}" \
            --repo "${{ github.repository }}" \
            --pattern "*.tar.gz" || true

      - name: Compute SHA256s
        id: sha256
        run: |
          for PLATFORM in amd64_sequoia arm64_sequoia; do
            FILE="pinc-${VERSION}.${PLATFORM}.bottle.tar.gz"
            if [ -f "${FILE}" ]; then
              SHA=$(sha256sum "${FILE}" | awk '{print $1}')
              echo "${PLATFORM//-/_}=${SHA}" >> $GITHUB_OUTPUT
            fi
          done

      - name: Update formula
        run: |
          BOTTLE_LINES=""
          if [ "${{ needs.check-assets.outputs.has_arm64_sequoia }}" == "true" ]; then
            BOTTLE_LINES="${BOTTLE_LINES}    sha256 cellar: :any_skip_relocation, arm64_sequoia: \"${{ steps.sha256.outputs.arm64_sequoia }}\"\n"
          fi
          if [ "${{ needs.check-assets.outputs.has_amd64_sequoia }}" == "true" ]; then
            BOTTLE_LINES="${BOTTLE_LINES}    sha256 cellar: :any_skip_relocation, sequoia: \"${{ steps.sha256.outputs.amd64_sequoia }}\"\n"
          fi

          export BASE_URL
          export BOTTLE_LINES=$(printf "${BOTTLE_LINES}")
          envsubst < .github/workflows/templates/pinc.rb.template > Formula/pinc.rb

      - name: Commit and push formula
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/pinc.rb
          git commit -m "pinc ${VERSION}"
          git push
